:: default
:: music
#

# modes
default < cmd + shift - m ; music
music < cmd + shift - m ; default

# swap windows
# default, music < shift + alt - h : yabai -m window --swap west
# default, music < shift + alt - j : yabai -m window --swap south
# default, music < shift + alt - k : yabai -m window --swap north
# default, music < shift + alt - l : yabai -m window --swap east

# focus window
default, music < cmd - h : yabai -m window --focus west
default, music < cmd - j : yabai -m window --focus south
default, music < cmd - k : yabai -m window --focus north
default < cmd - l        : yabai -m window --focus east
music < cmd + shift - l  : yabai -m window --focus east

# float / unfloat window and center on screen
default, music < cmd - t : yabai -m window --toggle float --grid 4:4:1:1:2:2

# move window
default, music < shift + cmd - h : yabai -m window --warp west
default, music < shift + cmd - j : yabai -m window --warp south
default, music < shift + cmd - k : yabai -m window --warp north
default, music < shift + cmd - l : yabai -m window --warp east

# rotate
default, music < cmd - r : yabai -m space --rotate 90

# mirror
default, music < cmd - y : yabai -m space --mirror y-axis

# open iTerm2
default, music < cmd - return : sh ~/.config/shellscripts/openiterm.sh
# cmd - return : open -a 'iTerm 2' --args "Kristian"

# toggle fullscreen
default, music < cmd - f : yabai -m window --toggle zoom-fullscreen

# move window to different space
default, music < shift + cmd - 1 : yabai -m window --space 1
default, music < shift + cmd - 2 : yabai -m window --space 2
default, music < shift + cmd - 3 : yabai -m window --space 3
default, music < shift + cmd - 4 : yabai -m window --space 4
default, music < shift + cmd - 5 : yabai -m window --space 5
default, music < shift + cmd - 6 : yabai -m window --space 6
default, music < shift + cmd - 7 : yabai -m window --space 7
default, music < shift + cmd - 8 : yabai -m window --space 8
default, music < shift + cmd - 9 : yabai -m window --space 9

# resize windows
default, music < alt + shift - h : yabai -m window --resize left:-20:0; \
                  yabai -m window --resize right:-20:0
default, music < alt + shift - j : yabai -m window --resize bottom:0:20; \
                  yabai -m window --resize top:0:20
default, music < alt + shift - k : yabai -m window --resize top:0:-20; \
                  yabai -m window --resize bottom:0:-20
default, music < alt + shift - l : yabai -m window --resize right:20:0; \
                  yabai -m window --resize left:20:0

default, music < cmd + alt - h : yabai -m display --focus prev
default, music < cmd + alt - j : yabai -m display --focus recent
